workflows:
  android-kotlin-workflow:
    name: Build Black Blue App
    max_build_duration: 30
    instance_type: mac_mini_m1
    environment:
      java: 17
    triggering:
      events:
        - push
    scripts:
      - name: Setup Android Structure
        script: |
          mkdir -p app/src/main/java/com/devkurdi/black
          cp MainActivity.kt app/src/main/java/com/devkurdi/black/
          echo "sdk.dir=$ANDROID_SDK_ROOT" > local.properties
          echo '
          plugins {
              id "com.android.application" version "8.1.1" apply false
              id "org.jetbrains.kotlin.android" version "1.8.10" apply false
          }
          ' > build.gradle
      - name: Build Android APK
        script: |
          echo "Building process started..."
    artifacts:
      - "*.kt"
